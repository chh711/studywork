# 梯度下降在干什么

如果把机器学习比作“调机器”，那损失函数就是仪表盘上的误差数字：它告诉我们现在的答案离目标有多远。梯度下降做的事，其实很朴素——不停地找“往哪边改，误差会变小”。

你可以把它想成在山里下坡。山谷最低处代表最小误差，我们站在山坡上，看不清全局，但能感觉到脚下哪边更陡。梯度就是这个“最陡方向”的信息。既然目标是下山，就不要往最陡上坡走，而要往相反方向迈一步。每走一步，就重新观察当前的位置，再决定下一步怎么走。

所以它不是一次算出最优答案，而是一个反复试探、逐步修正的过程。步子太大，可能直接跨过山谷，来回震荡；步子太小，又会走得很慢。学习率就像步子大小，合适最重要。

在机器学习里，模型参数就是那双“脚”的位置。参数变一点，损失就变一点；梯度下降不断调整参数，让模型的预测越来越接近真实答案。它的本质不是“聪明地猜”，而是“有方向地改”。

如果再把概率放进来理解，就更完整了：现实数据常常带噪声，答案不是绝对的。损失函数负责量化差距，梯度下降负责找改进方向，概率负责描述不确定性。三者合起来，机器学习才既能“知道差多少”，又能“知道往哪改”，还知道“世界本来就有随机性”。
